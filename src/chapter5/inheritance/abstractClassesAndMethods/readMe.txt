RULES:
1. abstract class can include nonabstract methods and variables  | Dosn't compile
2. abstract class is not required to include any abstract methods  | Dosn't compile
3. abstract method can be defined only in abstract class  | Dosn't compile
4. abstract methods cannot have implementation or body at all  | Dosn't compile
5. abstract classes and methods can't be defined as final  | Dosn't compile
6. method may not be defined as abstract and private at the same time  | Dosn't compile
7. when FIRS concrete/nonabstract class is extending an abstract class it must IMPLEMENT all abstract methods
8. abstract class can extend another abstract class, and it's not required to implement any of the abstract methods
9. concrete subclass is not required to implement for an abstract method if an intermediate CLASS provides